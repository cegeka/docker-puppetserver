---
{{- range .Values.image_streams }}
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  generation: 1
  name: {{.}}
spec:
  lookupPolicy:
    local: false
status:
  dockerImageRepository: ""
---
{{- end }}

apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  generation: 1
  name: puppetserver-code
spec:
  lookupPolicy:
    local: false
status:
  dockerImageRepository: ""
---

{{- range .Values.environments }}
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  generation: 1
  name: puppetserver-code-{{.}}
spec:
  lookupPolicy:
    local: false
status:
  dockerImageRepository: ""
---
{{- end }}
